section.section
  .container.mt-5
    .row
      .col-12.col-sm-8.offset-sm-2.col-md-6.offset-md-3.col-lg-6.offset-lg-3.col-xl-4.offset-xl-4
        .login-brand
          | Proyecto
        .card.card-primary
          .card-header
            h4 Iniciar sesión
          .card-body

            form( [formGroup]="form", (ngSubmit)="login()" )
              .form-group( [ngClass]="this._formResources.displayFieldCss('email', this.form)" )
                label(for='email') Email
                input#email.form-control( (change)="this.prueba()", type='email',formControlName="email" ,name='email', tabindex='1' )
                app-field-error( *ngIf="this._formResources.isFieldValid('email', this.form) && this.form.get('email').errors.required ", [errorMsg]="this.errorFields.email.required" )
                app-field-error( *ngIf="this._formResources.isFieldValid('email', this.form) && this.form.get('email').errors.email ", [errorMsg]="this.errorFields.email.email" )
                //- | {{ this.form.value.email }}
              .form-group( [ngClass]="this._formResources.displayFieldCss('pass', this.form)" )
                label.control-label(for='pass') Contraseña
                .input-group
                  input#pass.form-control( [type]="this.passHide ? 'text' : 'password'" ,formControlName="pass" ,name='pass', tabindex='2' )
                  .input-group-append
                    span.input-group-text
                      i.fa( [ngClass]="this._formResources.displayPassHide(this.passHide)" ,(click)="toggleFieldTextType()" )
                app-field-error( *ngIf="this._formResources.isFieldValid('pass', this.form) && this.form.get('pass').errors.required ", [errorMsg]="this.errorFields.pass.required" )

              .form-group
                button.btn.btn-primary.btn-lg.btn-block(type='submit' tabindex='4', [disabled]="!form.valid")
                  | Entrar



        .mt-5.text-muted.text-center
          | ¿No tienes cuenta?
          a&attributes({
            "[routerLink]": "['/register']",
            "routerLinkActive": "active"
          }) Registrate