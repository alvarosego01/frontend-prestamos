.navbar-bg
nav.navbar.navbar-expand-lg.main-navbar( [ngClass]="{'allNav': this._sessionService.estaLogueado() == false }")
	div.mr-auto( style="display: flex" )
		ul.navbar-nav.mr-3( [ngClass]="{'hideButton': this._sessionService.estaLogueado() == false}" )
			li
				a.nav-link.nav-link-lg( data-toggle='sidebar', style="color: white")
					i.fas.fa-bars
		a.navbar-brand( style="color: white" ) Proyecto
		.nav-collapse(*ngIf="this._sessionService.estaLogueado() == false")
			a.sidebar-gone-show.nav-collapse-toggle.nav-link(style="color: white")
				i.fas.fa-bars
			ul.navbar-nav
				li.nav-item
					a.nav-link( [routerLink]="['/register']", routerLinkActive="active") Registro de usuario
				li.nav-item
					a.nav-link( [routerLink]="['/login']", routerLinkActive="active") Iniciar sesión

	ul.navbar-nav.navbar-right(*ngIf="this._sessionService.estaLogueado() == true")

		li.dropdown
			a.nav-link.dropdown-toggle.nav-link-lg.nav-link-user(href='#' data-toggle='dropdown')
				img.rounded-circle.mr-1(alt='image' src='assets/img/avatar/avatar-1.png')
				.d-sm-none.d-lg-inline-block {{ this._sessionService.usuario.name }} {{ this._sessionService.usuario.last_name }}
			.dropdown-menu.dropdown-menu-right
				.dropdown-title

				.dropdown-divider
				a.dropdown-item.has-icon.text-danger( (click)="this.logout()" )
					i.fas.fa-sign-out-alt
					|  Cerrar sesión
